/**
 * Headless balancing simulator — real implementation lands in M1
 * (ROADMAP: "Headless-Simulator" once core/combat exists).
 * Usage (from M1 on): npm run sim -- --deck starter --enemy blighted_boar --n 1000
 */
console.error('sim: not implemented yet — combat core arrives in M1.');
process.exit(1);
